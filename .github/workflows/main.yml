on: [push]

jobs:
    build:
        runs-on: ubuntu-latest
        name: 
        steps:
            - uses: actions/checkout@v2
            - name: Build Docker image
              run: make build
            - name: Test Docker image
              run: make test
